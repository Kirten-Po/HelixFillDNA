"""
Единая точка правды о версии приложения.

Держится отдельным модулем, а не константой внутри gui/app.py, чтобы:
  * GUI мог показать версию в заголовке окна;
  * сборка релиза могла сверить её с git-тегом (см. .github/workflows/
    release.yml — шаг "Check version matches tag"), а не выпустить
    установщик 1.0.5, внутри которого приложение подписано как 1.0.4.

При выпуске новой версии значение ниже правится ВМЕСТЕ с созданием тега
vX.Y.Z — номера должны совпадать.
"""

__version__ = "1.2.0"
